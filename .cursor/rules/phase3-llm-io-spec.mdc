---
description: 阶段3后端 LLM 接入、NL2SQL Agent 与流式/函数调用字段规范（ChatQwQ + create_agent + SSE 映射）
globs: data-analysis/backend/**
alwaysApply: false
---

# 阶段3：LLM 输入输出字段规范

研发阶段3（`data-analysis/backend/`）时**必须**遵循本规范。字段约定来自 `test_chat_qwq.py` 对 `qwen3.7-max` + `ChatQwQ` 的实测结果。

## 1. LLM 客户端（`app/llm/client.py`）

| 项 | 规范 |
|---|---|
| 组件 | `langchain_qwq.ChatQwQ`（**禁止**默认国际端点；**禁止**未实测即用 `ChatOpenAI` 替代） |
| 包 | `langchain-qwq`（已在 `requirements.txt`） |
| 配置来源 | `app.config.settings` ← `backend/.env` |
| 环境变量 | `DASHSCOPE_API_KEY`、`LLM_MODEL`、`LLM_BASE_URL` |
| 国内端点 | `LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1`（国内 Key 必填；默认 `dashscope-intl` 会 401） |

```python
from langchain_qwq import ChatQwQ
from app.config import settings

def get_llm() -> ChatQwQ:
    return ChatQwQ(
        model=settings.llm_model,
        api_key=settings.dashscope_api_key,
        base_url=settings.llm_base_url,
        max_retries=2,
        streaming=True,
    )
```

## 2. 流式 chunk 字段（`llm.stream()` / `llm.astream()`）

`qwen3.7-max` 流式分两阶段：**先推理、后回答**。

| 字段路径 | 类型 | 阶段 | 用途 |
|---|---|---|---|
| `chunk.content` | `str` | 回答阶段 | **用户可见正文**；推理阶段通常为空字符串 |
| `chunk.additional_kwargs["reasoning_content"]` | `str` | 推理阶段 | 模型内部思考链；**默认不推给前端** |
| `chunk.tool_call_chunks` | `list` | 工具调用时 | 流式工具参数片段；Agent 层可忽略，以最终 `tool_calls` 为准 |
| `chunk.usage_metadata` | `dict \| None` | 最后一包 | `input_tokens`、`output_tokens`、`total_tokens`、`output_token_details.reasoning` |
| `chunk.response_metadata` | `dict` | 末包含 `finish_reason` | `finish_reason`（`stop` / `tool_calls`）、`model_name`、`model_provider` |

**流式读取模板**（SSE `token` 事件只允许推 `content`）：

```python
for chunk in llm.stream(messages):
    piece = chunk.content if isinstance(chunk.content, str) else ""
    if piece:
        yield {"event": "token", "data": piece}
    # reasoning_content 仅日志/调试，不写入 SSE token
```

## 3. 非流式 AIMessage 字段（`llm.invoke()` / 流式累加后）

| 字段路径 | 类型 | 说明 |
|---|---|---|
| `msg.content` | `str` | 最终自然语言回复；**发起 tool_calls 时可能为空** |
| `msg.additional_kwargs["reasoning_content"]` | `str` | 完整推理链 |
| `msg.tool_calls` | `list[dict]` | 每项：`name`、`args`（已解析 dict）、`id`、`type`（`"tool_call"`） |
| `msg.invalid_tool_calls` | `list` | 正常应为 `[]`；`langchain-qwq` 会修复畸形 JSON |
| `msg.usage_metadata` | `dict` | 同 chunk 用量字段 |
| `msg.response_metadata` | `dict` | `finish_reason`、`model_name`、`model_provider` |

`finish_reason == "tool_calls"` 时必须先执行工具，再二次 `invoke`，不可直接把空 `content` 当最终答案。

## 4. 函数调用（Tool Calling）往返规范

### 4.1 绑定与解析

```python
llm_with_tools = get_llm().bind_tools([run_sql, ...])
ai_msg = llm_with_tools.invoke(messages)
```

从 `ai_msg.tool_calls` 读取（**不要**从 `content` 解析 JSON）：

```python
for tc in ai_msg.tool_calls:
    name: str = tc["name"]
    args: dict = tc["args"]      # 已是 dict，勿 json.loads 字符串
    call_id: str = tc["id"]
```

### 4.2 工具执行与 ToolMessage 回传

| 字段 | 规范 |
|---|---|
| `ToolMessage.content` | `str(result)`（标量）；`json.dumps(result, ensure_ascii=False)`（dict/list） |
| `ToolMessage.tool_call_id` | **必须**等于对应 `tool_calls[].id` |
| `ToolMessage.name` | 工具名，与 `tool_calls[].name` 一致 |

```python
from langchain_core.messages import ToolMessage
import json

result = tool_fn.invoke(tc["args"])
tool_msg = ToolMessage(
    content=json.dumps(result, ensure_ascii=False) if isinstance(result, (dict, list)) else str(result),
    tool_call_id=tc["id"],
    name=tc["name"],
)
messages = [*messages, ai_msg, tool_msg]
final_msg = llm_with_tools.invoke(messages)
# 用户可见答案取 final_msg.content
```

### 4.3 SQL Agent 特别注意

- NL2SQL 采用 `create_agent` + 自定义 SQL 工具（**禁止** `create_sql_query_chain` / `langchain-classic`）
- SQL 文本只从 `tool_calls[].args["query"]` 提取（`name == "sql_db_query"`），**不要**从 `content` / `reasoning_content` 解析
- 工具返回 dict/list 时 `json.dumps(ensure_ascii=False)` 写入 `ToolMessage.content`（LangGraph 自动序列化）
- 图表 Agent 输入：从 `sql_db_query` 的 `ToolMessage.content` 解析 `result`，**不要**把 `reasoning_content` 传给下游
- 完整 NL2SQL 参数见 **§8**

## 5. SSE 事件与 LLM 字段映射（`api/chat.py`）

| SSE event | 数据来源 | 禁止 |
|---|---|---|
| `sql` | `tool_calls[].args["query"]`（`name == "sql_db_query"` 或 `sql_db_query_checker`） | 不要用 `reasoning_content` |
| `result` | `sql_db_query` 的 `ToolMessage.content` 解析后 `result` 字段（`columns`/`rows`/`row_count`） | 不要混在 `token` 里 |
| `token` | 仅 `chunk.content` 或 `final_msg.content` 增量 | 不要推 `reasoning_content` |
| `chart` | chart_agent 解析后的 ECharts option JSON | 不要混在 `content` 里 |
| `done` | 流结束；可附 `usage_metadata` 摘要 | — |
| `error` | 异常信息 | — |

事件体建议 JSON：`{"type":"token","content":"..."}`，与前端 `useChatStream.ts` 约定一致。

## 6. 会话记忆（`memory/history.py`）

持久化到 `messages` 表时：

| 角色 | 存储字段 |
|---|---|
| `human` | 用户原文 |
| `ai` | `content`（用户可见部分）；可选另存 `reasoning_content` 到 metadata JSON |
| `tool` | `ToolMessage.content` + `tool_call_id` |

加载历史构造 LangChain messages 时还原 `HumanMessage` / `AIMessage` / `ToolMessage`，**ToolMessage 必须带 `tool_call_id`**，否则多轮 tool calling 失败。

## 7. 阶段3实现自检

- [ ] `get_llm()` 使用 `ChatQwQ` + `settings.llm_base_url`（国内端点）
- [ ] `sql_agent` 使用 `create_agent` + §8 四个 SQL 工具，工具入参/出参形状一致
- [ ] SSE `token` 只发送 `content`，不泄露 `reasoning_content`
- [ ] SSE `sql` 来自 `tool_calls[].args["query"]`，不来自推理链
- [ ] `tool_calls[].args` 按 dict 使用；`ToolMessage.tool_call_id` 与 `id` 对齐
- [ ] `sql_db_query` 返回 `{"query", "result": {columns, rows, row_count}}` 结构
- [ ] `finish_reason == "tool_calls"` 时走完整工具往返，不提前结束 SSE
- [ ] SQLite 连接 `check_same_thread=False` + 工具层加锁（LangGraph ToolNode 线程池）
- [ ] 单测：`test_chat_qwq.py`（LLM 字段）、`test_nl2sql_agent_qwq.py`（NL2SQL 参数）

参考实测脚本：`data-analysis/backend/test_chat_qwq.py`、`data-analysis/backend/test_nl2sql_agent_qwq.py`

## 8. NL2SQL SQL Agent 参数规范（`create_agent` + SQL Tools）

字段约定来自 `test_nl2sql_agent_qwq.py` 对 `qwen3.7-max` + `ChatQwQ` + LangChain 1.3.9 `create_agent` 的实测结果。

### 8.1 组件与入口

| 项 | 规范 |
|---|---|
| Agent 工厂 | `langchain.agents.create_agent`（返回 LangGraph `CompiledStateGraph`） |
| 模型 | `get_llm()` → `ChatQwQ`（须支持 tool calling） |
| 工具定义 | `@tool` 装饰器，**必须**有 docstring（否则 LangChain 抛 `ValueError`） |
| 禁止 | `create_sql_query_chain`、`SQLDatabaseToolkit`（`langchain-classic` 遗留方案） |

```python
from langchain.agents import create_agent
from langchain.tools import tool

agent = create_agent(
    get_llm(),
    tools=[sql_db_list_tables, sql_db_schema, sql_db_query, sql_db_query_checker],
    system_prompt="...",
)
```

### 8.2 Agent 调用输入 / 输出

**输入**（`agent.invoke`）：

```python
{"messages": [HumanMessage(content="<用户自然语言问题>")]}
```

**输出**（`agent.invoke` 返回值）：

```python
{
  "messages": [
    HumanMessage(...),
    AIMessage(...),      # 可能多轮，含 tool_calls
    ToolMessage(...),    # 工具执行结果
  ],
  "structured_response": None,  # 未配置 response_format 时为 None
}
```

业务层从 `out["messages"]` 遍历提取 `AIMessage.tool_calls` 与 `ToolMessage.content`。

### 8.3 SQL 工具清单与 tool_calls 入参形状

每个 `tool_call` 统一结构（`qwen3.7-max` 实测）：

```json
{
  "name": "<tool_name>",
  "args": { },
  "id": "call_xxxxxxxx",
  "type": "tool_call"
}
```

| 工具名 | `args` 形状 | 说明 |
|---|---|---|
| `sql_db_list_tables` | `{}` | 无参；列出业务表 |
| `sql_db_schema` | `{"table_name": "users"}` | 获取表结构 + 样例行 |
| `sql_db_query_checker` | `{"query": "SELECT ..."}` | 执行前校验（可选，模型常会调用） |
| `sql_db_query` | `{"query": "SELECT ..."}` | **执行 SQL**；SSE `sql` 事件取此 `args.query` |

**关键约束**：

- `args` 已是 `dict`，**禁止** `json.loads(tc["args"])`（除非 `invalid_tool_calls`）
- `args["query"]` 为完整 SQL 字符串，含换行/分号由模型决定
- `sql_db_list_tables` 的 `args` 为空对象 `{}`，不是 `null`

### 8.4 工具返回值 → ToolMessage.content 形状

LangGraph ToolNode 将工具 Python 返回值序列化为 `ToolMessage.content`（**字符串**）。业务层需 `json.loads` 后再用。

#### `sql_db_list_tables`

```json
["orders", "users"]
```

#### `sql_db_schema`

```json
{
  "table": "users",
  "columns": [
    {"cid": 0, "name": "id", "type": "INTEGER", "notnull": false, "default": null, "pk": true}
  ],
  "sample_rows": {
    "columns": ["id", "name", "city"],
    "rows": [[1, "张三", "上海"]],
    "row_count": 3
  }
}
```

未知表时返回：`{"error": "unknown_table: <name>"}`

#### `sql_db_query_checker`

```json
{"ok": true, "reason": "ok", "query": "SELECT ..."}
```

不安全 SQL：`{"ok": false, "reason": "只允许 SELECT 开头的查询", "query": "..."}`

#### `sql_db_query`（**图表 Agent 与 SSE `result` 的数据源**）

成功：

```json
{
  "query": "SELECT city, COUNT(*) AS user_count FROM users GROUP BY city ORDER BY user_count DESC",
  "result": {
    "columns": ["city", "user_count"],
    "rows": [["上海", 2], ["北京", 1]],
    "row_count": 2
  }
}
```

失败：

```json
{"error": "unsafe_sql: 只允许 SELECT，禁止写操作关键字", "query": "..."}
```

或：

```json
{"error": "sql_error: OperationalError: ...", "query": "..."}
```

**`result` 字段规范**（`sql_db_query` 成功时）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `columns` | `list[str]` | 列名，顺序与 SQL 一致 |
| `rows` | `list[list[Any]]` | 行数据；数值保持 number，文本为 str |
| `row_count` | `int` | `len(rows)` |

### 8.5 典型多轮 tool_calls 时序（实测）

以「统计每个城市的用户数量」为例，模型实际调用顺序：

```
HumanMessage
  → AIMessage(tool_calls: sql_db_list_tables, finish_reason=tool_calls)
  → ToolMessage(content: ["orders","users"])
  → AIMessage(tool_calls: sql_db_schema{table_name:users}, finish_reason=tool_calls)
  → ToolMessage(content: {table, columns, sample_rows})
  → AIMessage(tool_calls: sql_db_query_checker{query:...}, finish_reason=tool_calls)  # 可选
  → ToolMessage(content: {ok, reason, query})
  → AIMessage(tool_calls: sql_db_query{query:...}, finish_reason=tool_calls)
  → ToolMessage(content: {query, result})
  → AIMessage(content: "查询完成！...", finish_reason=stop)   # 最终用户可见答案
```

**阶段3实现要点**：

- 发起 tool_calls 的 `AIMessage.content` 通常为空；推理在 `additional_kwargs.reasoning_content`
- 最终答案取**最后一条** `finish_reason == "stop"` 的 `AIMessage.content`
- 联表查询时模型可能连续调用多次 `sql_db_schema`（如 `users` + `orders`）

### 8.6 AIMessage / ToolMessage 在 Agent 内的补充字段

与 §2、§3 一致，NL2SQL 场景额外关注：

| 场景 | 字段 | 实测值 |
|---|---|---|
| 工具调用轮 | `response_metadata.finish_reason` | `"tool_calls"` |
| 最终回答轮 | `response_metadata.finish_reason` | `"stop"` |
| 工具调用轮 | `content` | 通常 `""` |
| 工具调用轮 | `additional_kwargs.reasoning_content` | 含 SQL 构思过程，**不推前端** |
| ToolMessage | `tool_call_id` | 对齐 `tool_calls[].id` |
| ToolMessage | `name` | 对齐 `tool_calls[].name` |

### 8.7 SSE 事件与 NL2SQL 字段映射（`api/chat.py`）

在 Agent `invoke` / `stream` 过程中，按消息类型分发：

| SSE event | 触发时机 | payload 建议 |
|---|---|---|
| `sql` | 捕获 `tool_calls` 且 `name` 为 `sql_db_query` 或 `sql_db_query_checker` | `{"type":"sql","sql":"<args.query>"}` |
| `result` | 对应 `sql_db_query` 的 `ToolMessage` 到达 | `{"type":"result","columns":[...],"rows":[...],"row_count":n}` |
| `token` | 最终 `AIMessage` 的 `content` 流式增量 | `{"type":"token","content":"..."}` |
| `chart` | chart_agent 产出后 | `{"type":"chart","option":{...}}` |
| `done` | Agent 图结束 | `{"type":"done","usage":{...}}` 可选 |
| `error` | 工具返回 `error` 或异常 | `{"type":"error","message":"..."}` |

**提取 SQL 的代码模板**：

```python
for tc in ai_msg.tool_calls:
    if tc["name"] in ("sql_db_query", "sql_db_query_checker"):
        sql_text = tc["args"].get("query", "")
        yield {"event": "sql", "data": json.dumps({"type": "sql", "sql": sql_text}, ensure_ascii=False)}
```

**提取结果集的代码模板**：

```python
if isinstance(msg, ToolMessage) and msg.name == "sql_db_query":
    payload = json.loads(msg.content)
    if "result" in payload:
        yield {"event": "result", "data": json.dumps({"type": "result", **payload["result"]}, ensure_ascii=False)}
```

### 8.8 SQL 安全与 SQLite 并发

| 项 | 规范 |
|---|---|
| 仅允许 SELECT | `sql_db_query` 内白名单校验，拒绝 DML/DDL 关键字 |
| SQLite 线程 | `sqlite3.connect(path, check_same_thread=False)` |
| 工具并发 | LangGraph ToolNode 默认线程池；同一 connection 须 `threading.Lock` 串行访问 |
| 参数化 | 用户输入不进 SQL 字符串拼接；由模型生成完整 SQL，后端只做关键字拦截 |

### 8.9 `agents/sql_agent.py` 实现骨架

```python
# app/agents/sql_agent.py
from langchain.agents import create_agent
from langchain_core.messages import HumanMessage
from app.llm.client import get_llm
from app.agents.sql_tools import build_sql_tools  # 封装 §8.3 四工具

SYSTEM_PROMPT = """你是 SQL Agent。必须先 list_tables，必要时 schema，再 query。
只能 SELECT；写操作拒绝。"""

def create_sql_agent(db_conn):
    return create_agent(
        get_llm(),
        tools=build_sql_tools(db_conn),
        system_prompt=SYSTEM_PROMPT,
    )

def run_nl2sql(agent, question: str) -> list:
    out = agent.invoke({"messages": [HumanMessage(content=question)]})
    return out["messages"]
```
